# 3D Model Credits

The models below were found in `/public/models/` at build time. Replace each
placeholder with the exact credit line from the model's source page (on
Sketchfab use the **"Copy Credits"** button), then mirror the same text in
`app/credits/page.tsx` so the footer credits link stays accurate.

| File | Used in | Credit (fill in) |
| --- | --- | --- |
| `dumbbell.glb` (~4.2 MB) | Hero | Dumbbell — author: ___ — license: ___ |
| `kettlebell.glb` (~3.4 MB) | Premium Access | Kettlebell — author: ___ — license: ___ |
| `weight_lifting_set.glb` (~2.2 MB) | Membership accent | Weight Lifting Set — author: ___ — license: ___ |

Notes:

- CC0 models need no attribution, but listing them anyway is good practice.
- CC-Attribution (CC-BY) models **must** be credited — name, author, license.
- Models are linked in the site footer via the "3D models: credits" link (`/credits`).
